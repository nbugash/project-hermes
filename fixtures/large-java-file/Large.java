package vega.fixtures.large;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

/** Generated fixture. Do not edit by hand; see fixtures/build.gradle.kts. */
public final class Large {

    /** Fixture type 0. */
    public static final class Node0<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-0";

        public Node0(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 0, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1. */
    public static final class Node1<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1";

        public Node1(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 2. */
    public static final class Node2<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-2";

        public Node2(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 3. */
    public static final class Node3<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-3";

        public Node3(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 3, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 4. */
    public static final class Node4<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-4";

        public Node4(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 5. */
    public static final class Node5<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-5";

        public Node5(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 5, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 6. */
    public static final class Node6<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-6";

        public Node6(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 6, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 7. */
    public static final class Node7<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-7";

        public Node7(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 7, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 8. */
    public static final class Node8<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-8";

        public Node8(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 8, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 9. */
    public static final class Node9<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-9";

        public Node9(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 10. */
    public static final class Node10<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-10";

        public Node10(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 11. */
    public static final class Node11<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-11";

        public Node11(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 12. */
    public static final class Node12<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-12";

        public Node12(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 13. */
    public static final class Node13<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-13";

        public Node13(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 14. */
    public static final class Node14<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-14";

        public Node14(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 15. */
    public static final class Node15<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-15";

        public Node15(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 15, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 16. */
    public static final class Node16<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-16";

        public Node16(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 17. */
    public static final class Node17<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-17";

        public Node17(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 18. */
    public static final class Node18<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-18";

        public Node18(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 18, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 19. */
    public static final class Node19<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-19";

        public Node19(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 19, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 20. */
    public static final class Node20<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-20";

        public Node20(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 20, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 20, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 21. */
    public static final class Node21<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-21";

        public Node21(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 22. */
    public static final class Node22<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-22";

        public Node22(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 22, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 23. */
    public static final class Node23<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-23";

        public Node23(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 24. */
    public static final class Node24<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-24";

        public Node24(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 24, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 25. */
    public static final class Node25<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-25";

        public Node25(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 26. */
    public static final class Node26<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-26";

        public Node26(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 26, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 27. */
    public static final class Node27<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-27";

        public Node27(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 28. */
    public static final class Node28<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-28";

        public Node28(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 28, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 29. */
    public static final class Node29<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-29";

        public Node29(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 29, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 29, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 30. */
    public static final class Node30<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-30";

        public Node30(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 30, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 30, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 31. */
    public static final class Node31<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-31";

        public Node31(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 31, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 32. */
    public static final class Node32<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-32";

        public Node32(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 32, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 33. */
    public static final class Node33<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-33";

        public Node33(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 34. */
    public static final class Node34<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-34";

        public Node34(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 34, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 35. */
    public static final class Node35<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-35";

        public Node35(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 35, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 36. */
    public static final class Node36<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-36";

        public Node36(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 37. */
    public static final class Node37<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-37";

        public Node37(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 38. */
    public static final class Node38<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-38";

        public Node38(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 38, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 39. */
    public static final class Node39<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-39";

        public Node39(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 39, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 40. */
    public static final class Node40<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-40";

        public Node40(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 40, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 41. */
    public static final class Node41<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-41";

        public Node41(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 41, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 42. */
    public static final class Node42<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-42";

        public Node42(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 43. */
    public static final class Node43<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-43";

        public Node43(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 44. */
    public static final class Node44<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-44";

        public Node44(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 44, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 45. */
    public static final class Node45<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-45";

        public Node45(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 46. */
    public static final class Node46<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-46";

        public Node46(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 47. */
    public static final class Node47<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-47";

        public Node47(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 48. */
    public static final class Node48<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-48";

        public Node48(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 49. */
    public static final class Node49<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-49";

        public Node49(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 50. */
    public static final class Node50<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-50";

        public Node50(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 51. */
    public static final class Node51<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-51";

        public Node51(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 52. */
    public static final class Node52<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-52";

        public Node52(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 53. */
    public static final class Node53<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-53";

        public Node53(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 53, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 53, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 54. */
    public static final class Node54<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-54";

        public Node54(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 55. */
    public static final class Node55<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-55";

        public Node55(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 55, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 56. */
    public static final class Node56<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-56";

        public Node56(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 57. */
    public static final class Node57<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-57";

        public Node57(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 58. */
    public static final class Node58<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-58";

        public Node58(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 58, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 59. */
    public static final class Node59<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-59";

        public Node59(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 59, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 60. */
    public static final class Node60<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-60";

        public Node60(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 61. */
    public static final class Node61<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-61";

        public Node61(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 61, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 61, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 62. */
    public static final class Node62<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-62";

        public Node62(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 63. */
    public static final class Node63<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-63";

        public Node63(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 64. */
    public static final class Node64<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-64";

        public Node64(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 64, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 65. */
    public static final class Node65<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-65";

        public Node65(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 65, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 65, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 66. */
    public static final class Node66<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-66";

        public Node66(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 67. */
    public static final class Node67<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-67";

        public Node67(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 68. */
    public static final class Node68<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-68";

        public Node68(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 68, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 69. */
    public static final class Node69<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-69";

        public Node69(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 69, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 70. */
    public static final class Node70<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-70";

        public Node70(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 70, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 71. */
    public static final class Node71<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-71";

        public Node71(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 72. */
    public static final class Node72<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-72";

        public Node72(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 73. */
    public static final class Node73<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-73";

        public Node73(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 74. */
    public static final class Node74<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-74";

        public Node74(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 74, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 74, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 75. */
    public static final class Node75<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-75";

        public Node75(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 76. */
    public static final class Node76<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-76";

        public Node76(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 76, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 77. */
    public static final class Node77<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-77";

        public Node77(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 77, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 77, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 78. */
    public static final class Node78<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-78";

        public Node78(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 78, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 79. */
    public static final class Node79<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-79";

        public Node79(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 79, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 80. */
    public static final class Node80<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-80";

        public Node80(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 80, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 80, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 81. */
    public static final class Node81<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-81";

        public Node81(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 81, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 82. */
    public static final class Node82<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-82";

        public Node82(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 82, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 83. */
    public static final class Node83<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-83";

        public Node83(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 84. */
    public static final class Node84<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-84";

        public Node84(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 84, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 85. */
    public static final class Node85<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-85";

        public Node85(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 85, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 86. */
    public static final class Node86<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-86";

        public Node86(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 86, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 87. */
    public static final class Node87<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-87";

        public Node87(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 87, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 88. */
    public static final class Node88<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-88";

        public Node88(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 88, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 88, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 89. */
    public static final class Node89<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-89";

        public Node89(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 89, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 90. */
    public static final class Node90<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-90";

        public Node90(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 90, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 91. */
    public static final class Node91<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-91";

        public Node91(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 91, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 92. */
    public static final class Node92<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-92";

        public Node92(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 93. */
    public static final class Node93<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-93";

        public Node93(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 93, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 94. */
    public static final class Node94<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-94";

        public Node94(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 94, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 95. */
    public static final class Node95<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-95";

        public Node95(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 96. */
    public static final class Node96<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-96";

        public Node96(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 96, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 96, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 97. */
    public static final class Node97<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-97";

        public Node97(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 97, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 98. */
    public static final class Node98<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-98";

        public Node98(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 99. */
    public static final class Node99<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-99";

        public Node99(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 100. */
    public static final class Node100<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-100";

        public Node100(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 100, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 100, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 101. */
    public static final class Node101<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-101";

        public Node101(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 102. */
    public static final class Node102<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-102";

        public Node102(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 103. */
    public static final class Node103<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-103";

        public Node103(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 104. */
    public static final class Node104<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-104";

        public Node104(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 105. */
    public static final class Node105<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-105";

        public Node105(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 106. */
    public static final class Node106<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-106";

        public Node106(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 106, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 106, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 107. */
    public static final class Node107<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-107";

        public Node107(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 108. */
    public static final class Node108<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-108";

        public Node108(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 109. */
    public static final class Node109<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-109";

        public Node109(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 109, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 110. */
    public static final class Node110<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-110";

        public Node110(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 110, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 110, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 111. */
    public static final class Node111<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-111";

        public Node111(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 111, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 112. */
    public static final class Node112<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-112";

        public Node112(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 112, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 113. */
    public static final class Node113<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-113";

        public Node113(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 114. */
    public static final class Node114<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-114";

        public Node114(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 114, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 115. */
    public static final class Node115<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-115";

        public Node115(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 116. */
    public static final class Node116<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-116";

        public Node116(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 116, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 117. */
    public static final class Node117<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-117";

        public Node117(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 118. */
    public static final class Node118<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-118";

        public Node118(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 119. */
    public static final class Node119<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-119";

        public Node119(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 119, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 120. */
    public static final class Node120<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-120";

        public Node120(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 121. */
    public static final class Node121<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-121";

        public Node121(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 122. */
    public static final class Node122<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-122";

        public Node122(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 123. */
    public static final class Node123<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-123";

        public Node123(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 124. */
    public static final class Node124<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-124";

        public Node124(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 125. */
    public static final class Node125<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-125";

        public Node125(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 126. */
    public static final class Node126<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-126";

        public Node126(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 127. */
    public static final class Node127<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-127";

        public Node127(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 127, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 127, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 128. */
    public static final class Node128<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-128";

        public Node128(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 128, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 129. */
    public static final class Node129<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-129";

        public Node129(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 130. */
    public static final class Node130<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-130";

        public Node130(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 131. */
    public static final class Node131<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-131";

        public Node131(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 131, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 131, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 132. */
    public static final class Node132<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-132";

        public Node132(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 133. */
    public static final class Node133<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-133";

        public Node133(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 133, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 134. */
    public static final class Node134<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-134";

        public Node134(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 134, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 134, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 135. */
    public static final class Node135<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-135";

        public Node135(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 135, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 136. */
    public static final class Node136<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-136";

        public Node136(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 137. */
    public static final class Node137<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-137";

        public Node137(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 138. */
    public static final class Node138<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-138";

        public Node138(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 138, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 139. */
    public static final class Node139<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-139";

        public Node139(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 140. */
    public static final class Node140<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-140";

        public Node140(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 140, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 141. */
    public static final class Node141<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-141";

        public Node141(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 141, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 141, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 142. */
    public static final class Node142<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-142";

        public Node142(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 143. */
    public static final class Node143<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-143";

        public Node143(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 144. */
    public static final class Node144<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-144";

        public Node144(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 144, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 145. */
    public static final class Node145<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-145";

        public Node145(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 145, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 145, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 146. */
    public static final class Node146<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-146";

        public Node146(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 146, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 147. */
    public static final class Node147<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-147";

        public Node147(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 148. */
    public static final class Node148<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-148";

        public Node148(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 148, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 148, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 149. */
    public static final class Node149<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-149";

        public Node149(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 149, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 150. */
    public static final class Node150<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-150";

        public Node150(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 150, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 150, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 151. */
    public static final class Node151<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-151";

        public Node151(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 151, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 152. */
    public static final class Node152<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-152";

        public Node152(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 152, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 152, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 153. */
    public static final class Node153<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-153";

        public Node153(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 154. */
    public static final class Node154<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-154";

        public Node154(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 155. */
    public static final class Node155<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-155";

        public Node155(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 156. */
    public static final class Node156<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-156";

        public Node156(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 156, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 156, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 157. */
    public static final class Node157<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-157";

        public Node157(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 158. */
    public static final class Node158<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-158";

        public Node158(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 159. */
    public static final class Node159<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-159";

        public Node159(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 160. */
    public static final class Node160<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-160";

        public Node160(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 160, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 161. */
    public static final class Node161<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-161";

        public Node161(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 161, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 162. */
    public static final class Node162<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-162";

        public Node162(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 163. */
    public static final class Node163<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-163";

        public Node163(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 163, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 164. */
    public static final class Node164<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-164";

        public Node164(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 165. */
    public static final class Node165<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-165";

        public Node165(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 166. */
    public static final class Node166<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-166";

        public Node166(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 166, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 166, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 167. */
    public static final class Node167<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-167";

        public Node167(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 167, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 168. */
    public static final class Node168<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-168";

        public Node168(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 169. */
    public static final class Node169<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-169";

        public Node169(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 170. */
    public static final class Node170<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-170";

        public Node170(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 171. */
    public static final class Node171<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-171";

        public Node171(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 172. */
    public static final class Node172<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-172";

        public Node172(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 172, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 173. */
    public static final class Node173<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-173";

        public Node173(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 174. */
    public static final class Node174<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-174";

        public Node174(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 175. */
    public static final class Node175<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-175";

        public Node175(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 175, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 176. */
    public static final class Node176<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-176";

        public Node176(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 177. */
    public static final class Node177<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-177";

        public Node177(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 178. */
    public static final class Node178<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-178";

        public Node178(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 179. */
    public static final class Node179<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-179";

        public Node179(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 179, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 179, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 180. */
    public static final class Node180<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-180";

        public Node180(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 181. */
    public static final class Node181<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-181";

        public Node181(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 181, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 182. */
    public static final class Node182<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-182";

        public Node182(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 182, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 182, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 183. */
    public static final class Node183<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-183";

        public Node183(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 183, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 184. */
    public static final class Node184<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-184";

        public Node184(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 184, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 184, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 185. */
    public static final class Node185<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-185";

        public Node185(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 185, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 186. */
    public static final class Node186<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-186";

        public Node186(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 186, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 187. */
    public static final class Node187<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-187";

        public Node187(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 187, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 188. */
    public static final class Node188<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-188";

        public Node188(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 188, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 189. */
    public static final class Node189<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-189";

        public Node189(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 189, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 189, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 190. */
    public static final class Node190<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-190";

        public Node190(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 190, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 190, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 191. */
    public static final class Node191<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-191";

        public Node191(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 191, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 191, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 192. */
    public static final class Node192<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-192";

        public Node192(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 193. */
    public static final class Node193<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-193";

        public Node193(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 193, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 194. */
    public static final class Node194<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-194";

        public Node194(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 194, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 195. */
    public static final class Node195<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-195";

        public Node195(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 195, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 195, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 196. */
    public static final class Node196<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-196";

        public Node196(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 197. */
    public static final class Node197<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-197";

        public Node197(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 197, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 198. */
    public static final class Node198<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-198";

        public Node198(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 199. */
    public static final class Node199<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-199";

        public Node199(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 199, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 200. */
    public static final class Node200<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-200";

        public Node200(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 200, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 201. */
    public static final class Node201<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-201";

        public Node201(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 202. */
    public static final class Node202<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-202";

        public Node202(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 202, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 203. */
    public static final class Node203<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-203";

        public Node203(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 203, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 204. */
    public static final class Node204<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-204";

        public Node204(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 204, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 205. */
    public static final class Node205<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-205";

        public Node205(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 206. */
    public static final class Node206<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-206";

        public Node206(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 207. */
    public static final class Node207<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-207";

        public Node207(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 207, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 208. */
    public static final class Node208<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-208";

        public Node208(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 209. */
    public static final class Node209<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-209";

        public Node209(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 210. */
    public static final class Node210<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-210";

        public Node210(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 211. */
    public static final class Node211<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-211";

        public Node211(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 211, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 212. */
    public static final class Node212<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-212";

        public Node212(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 212, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 213. */
    public static final class Node213<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-213";

        public Node213(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 214. */
    public static final class Node214<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-214";

        public Node214(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 215. */
    public static final class Node215<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-215";

        public Node215(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 215, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 215, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 216. */
    public static final class Node216<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-216";

        public Node216(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 216, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 217. */
    public static final class Node217<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-217";

        public Node217(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 217, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 217, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 218. */
    public static final class Node218<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-218";

        public Node218(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 218, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 219. */
    public static final class Node219<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-219";

        public Node219(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 220. */
    public static final class Node220<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-220";

        public Node220(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 220, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 220, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 221. */
    public static final class Node221<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-221";

        public Node221(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 222. */
    public static final class Node222<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-222";

        public Node222(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 223. */
    public static final class Node223<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-223";

        public Node223(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 224. */
    public static final class Node224<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-224";

        public Node224(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 224, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 225. */
    public static final class Node225<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-225";

        public Node225(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 226. */
    public static final class Node226<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-226";

        public Node226(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 226, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 227. */
    public static final class Node227<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-227";

        public Node227(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 227, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 228. */
    public static final class Node228<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-228";

        public Node228(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 229. */
    public static final class Node229<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-229";

        public Node229(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 229, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 230. */
    public static final class Node230<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-230";

        public Node230(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 231. */
    public static final class Node231<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-231";

        public Node231(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 232. */
    public static final class Node232<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-232";

        public Node232(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 233. */
    public static final class Node233<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-233";

        public Node233(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 233, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 234. */
    public static final class Node234<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-234";

        public Node234(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 235. */
    public static final class Node235<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-235";

        public Node235(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 235, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 236. */
    public static final class Node236<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-236";

        public Node236(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 236, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 237. */
    public static final class Node237<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-237";

        public Node237(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 237, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 238. */
    public static final class Node238<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-238";

        public Node238(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 239. */
    public static final class Node239<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-239";

        public Node239(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 240. */
    public static final class Node240<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-240";

        public Node240(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 240, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 241. */
    public static final class Node241<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-241";

        public Node241(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 242. */
    public static final class Node242<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-242";

        public Node242(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 242, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 243. */
    public static final class Node243<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-243";

        public Node243(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 244. */
    public static final class Node244<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-244";

        public Node244(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 245. */
    public static final class Node245<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-245";

        public Node245(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 245, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 246. */
    public static final class Node246<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-246";

        public Node246(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 246, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 246, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 247. */
    public static final class Node247<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-247";

        public Node247(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 247, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 248. */
    public static final class Node248<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-248";

        public Node248(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 249. */
    public static final class Node249<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-249";

        public Node249(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 250. */
    public static final class Node250<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-250";

        public Node250(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 250, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 251. */
    public static final class Node251<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-251";

        public Node251(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 252. */
    public static final class Node252<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-252";

        public Node252(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 253. */
    public static final class Node253<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-253";

        public Node253(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 254. */
    public static final class Node254<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-254";

        public Node254(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 254, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 255. */
    public static final class Node255<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-255";

        public Node255(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 255, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 256. */
    public static final class Node256<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-256";

        public Node256(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 257. */
    public static final class Node257<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-257";

        public Node257(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 257, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 258. */
    public static final class Node258<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-258";

        public Node258(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 258, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 259. */
    public static final class Node259<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-259";

        public Node259(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 260. */
    public static final class Node260<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-260";

        public Node260(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 260, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 260, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 261. */
    public static final class Node261<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-261";

        public Node261(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 261, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 261, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 262. */
    public static final class Node262<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-262";

        public Node262(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 263. */
    public static final class Node263<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-263";

        public Node263(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 263, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 264. */
    public static final class Node264<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-264";

        public Node264(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 264, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 265. */
    public static final class Node265<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-265";

        public Node265(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 266. */
    public static final class Node266<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-266";

        public Node266(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 266, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 266, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 267. */
    public static final class Node267<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-267";

        public Node267(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 267, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 268. */
    public static final class Node268<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-268";

        public Node268(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 269. */
    public static final class Node269<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-269";

        public Node269(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 269, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 270. */
    public static final class Node270<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-270";

        public Node270(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 270, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 271. */
    public static final class Node271<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-271";

        public Node271(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 272. */
    public static final class Node272<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-272";

        public Node272(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 272, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 273. */
    public static final class Node273<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-273";

        public Node273(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 274. */
    public static final class Node274<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-274";

        public Node274(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 274, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 275. */
    public static final class Node275<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-275";

        public Node275(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 275, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 276. */
    public static final class Node276<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-276";

        public Node276(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 276, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 276, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 276, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 277. */
    public static final class Node277<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-277";

        public Node277(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 277, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 278. */
    public static final class Node278<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-278";

        public Node278(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 279. */
    public static final class Node279<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-279";

        public Node279(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 280. */
    public static final class Node280<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-280";

        public Node280(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 280, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 281. */
    public static final class Node281<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-281";

        public Node281(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 281, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 282. */
    public static final class Node282<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-282";

        public Node282(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 283. */
    public static final class Node283<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-283";

        public Node283(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 283, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 284. */
    public static final class Node284<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-284";

        public Node284(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 284, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 285. */
    public static final class Node285<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-285";

        public Node285(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 285, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 286. */
    public static final class Node286<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-286";

        public Node286(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 286, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 287. */
    public static final class Node287<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-287";

        public Node287(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 287, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 287, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 288. */
    public static final class Node288<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-288";

        public Node288(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 288, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 289. */
    public static final class Node289<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-289";

        public Node289(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 289, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 290. */
    public static final class Node290<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-290";

        public Node290(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 291. */
    public static final class Node291<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-291";

        public Node291(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 292. */
    public static final class Node292<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-292";

        public Node292(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 292, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 293. */
    public static final class Node293<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-293";

        public Node293(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 294. */
    public static final class Node294<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-294";

        public Node294(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 294, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 295. */
    public static final class Node295<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-295";

        public Node295(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 295, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 295, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 296. */
    public static final class Node296<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-296";

        public Node296(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 296, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 296, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 297. */
    public static final class Node297<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-297";

        public Node297(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 298. */
    public static final class Node298<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-298";

        public Node298(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 298, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 298, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 299. */
    public static final class Node299<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-299";

        public Node299(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 300. */
    public static final class Node300<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-300";

        public Node300(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 301. */
    public static final class Node301<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-301";

        public Node301(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 301, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 302. */
    public static final class Node302<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-302";

        public Node302(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 302, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 302, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 303. */
    public static final class Node303<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-303";

        public Node303(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 304. */
    public static final class Node304<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-304";

        public Node304(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 304, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 304, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 305. */
    public static final class Node305<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-305";

        public Node305(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 305, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 306. */
    public static final class Node306<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-306";

        public Node306(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 306, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 307. */
    public static final class Node307<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-307";

        public Node307(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 307, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 308. */
    public static final class Node308<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-308";

        public Node308(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 308, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 309. */
    public static final class Node309<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-309";

        public Node309(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 309, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 310. */
    public static final class Node310<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-310";

        public Node310(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 310, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 310, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 310, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 311. */
    public static final class Node311<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-311";

        public Node311(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 311, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 311, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 312. */
    public static final class Node312<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-312";

        public Node312(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 313. */
    public static final class Node313<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-313";

        public Node313(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 313, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 313, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 314. */
    public static final class Node314<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-314";

        public Node314(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 315. */
    public static final class Node315<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-315";

        public Node315(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 315, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 316. */
    public static final class Node316<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-316";

        public Node316(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 316, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 317. */
    public static final class Node317<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-317";

        public Node317(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 317, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 318. */
    public static final class Node318<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-318";

        public Node318(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 319. */
    public static final class Node319<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-319";

        public Node319(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 319, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 320. */
    public static final class Node320<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-320";

        public Node320(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 321. */
    public static final class Node321<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-321";

        public Node321(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 321, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 322. */
    public static final class Node322<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-322";

        public Node322(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 323. */
    public static final class Node323<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-323";

        public Node323(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 323, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 323, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 324. */
    public static final class Node324<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-324";

        public Node324(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 324, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 324, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 324, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 325. */
    public static final class Node325<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-325";

        public Node325(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 325, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 325, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 326. */
    public static final class Node326<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-326";

        public Node326(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 327. */
    public static final class Node327<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-327";

        public Node327(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 327, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 328. */
    public static final class Node328<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-328";

        public Node328(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 328, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 329. */
    public static final class Node329<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-329";

        public Node329(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 330. */
    public static final class Node330<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-330";

        public Node330(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 330, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 331. */
    public static final class Node331<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-331";

        public Node331(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 331, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 332. */
    public static final class Node332<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-332";

        public Node332(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 332, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 333. */
    public static final class Node333<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-333";

        public Node333(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 333, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 334. */
    public static final class Node334<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-334";

        public Node334(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 334, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 335. */
    public static final class Node335<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-335";

        public Node335(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 336. */
    public static final class Node336<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-336";

        public Node336(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 336, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 337. */
    public static final class Node337<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-337";

        public Node337(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 337, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 338. */
    public static final class Node338<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-338";

        public Node338(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 338, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 339. */
    public static final class Node339<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-339";

        public Node339(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 339, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 340. */
    public static final class Node340<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-340";

        public Node340(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 340, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 340, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 341. */
    public static final class Node341<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-341";

        public Node341(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 342. */
    public static final class Node342<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-342";

        public Node342(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 342, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 343. */
    public static final class Node343<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-343";

        public Node343(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 344. */
    public static final class Node344<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-344";

        public Node344(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 344, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 345. */
    public static final class Node345<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-345";

        public Node345(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 345, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 346. */
    public static final class Node346<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-346";

        public Node346(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 346, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 347. */
    public static final class Node347<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-347";

        public Node347(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 347, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 348. */
    public static final class Node348<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-348";

        public Node348(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 349. */
    public static final class Node349<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-349";

        public Node349(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 350. */
    public static final class Node350<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-350";

        public Node350(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 351. */
    public static final class Node351<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-351";

        public Node351(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 351, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 352. */
    public static final class Node352<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-352";

        public Node352(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 352, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 353. */
    public static final class Node353<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-353";

        public Node353(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 354. */
    public static final class Node354<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-354";

        public Node354(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 354, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 355. */
    public static final class Node355<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-355";

        public Node355(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 356. */
    public static final class Node356<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-356";

        public Node356(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 357. */
    public static final class Node357<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-357";

        public Node357(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 358. */
    public static final class Node358<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-358";

        public Node358(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 358, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 359. */
    public static final class Node359<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-359";

        public Node359(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 359, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 359, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 360. */
    public static final class Node360<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-360";

        public Node360(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 361. */
    public static final class Node361<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-361";

        public Node361(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 362. */
    public static final class Node362<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-362";

        public Node362(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 362, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 362, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 363. */
    public static final class Node363<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-363";

        public Node363(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 363, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 364. */
    public static final class Node364<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-364";

        public Node364(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 364, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 365. */
    public static final class Node365<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-365";

        public Node365(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 366. */
    public static final class Node366<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-366";

        public Node366(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 366, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 367. */
    public static final class Node367<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-367";

        public Node367(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 367, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 368. */
    public static final class Node368<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-368";

        public Node368(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 368, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 369. */
    public static final class Node369<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-369";

        public Node369(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 370. */
    public static final class Node370<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-370";

        public Node370(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 371. */
    public static final class Node371<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-371";

        public Node371(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 371, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 372. */
    public static final class Node372<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-372";

        public Node372(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 373. */
    public static final class Node373<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-373";

        public Node373(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 374. */
    public static final class Node374<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-374";

        public Node374(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 374, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 375. */
    public static final class Node375<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-375";

        public Node375(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 375, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 375, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 376. */
    public static final class Node376<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-376";

        public Node376(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 376, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 377. */
    public static final class Node377<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-377";

        public Node377(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 377, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 377, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 378. */
    public static final class Node378<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-378";

        public Node378(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 379. */
    public static final class Node379<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-379";

        public Node379(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 379, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 379, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 380. */
    public static final class Node380<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-380";

        public Node380(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 380, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 381. */
    public static final class Node381<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-381";

        public Node381(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 381, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 382. */
    public static final class Node382<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-382";

        public Node382(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 382, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 383. */
    public static final class Node383<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-383";

        public Node383(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 384. */
    public static final class Node384<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-384";

        public Node384(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 385. */
    public static final class Node385<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-385";

        public Node385(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 386. */
    public static final class Node386<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-386";

        public Node386(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 386, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 387. */
    public static final class Node387<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-387";

        public Node387(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 387, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 388. */
    public static final class Node388<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-388";

        public Node388(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 389. */
    public static final class Node389<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-389";

        public Node389(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 389, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 390. */
    public static final class Node390<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-390";

        public Node390(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 390, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 391. */
    public static final class Node391<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-391";

        public Node391(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 391, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 392. */
    public static final class Node392<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-392";

        public Node392(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 392, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 392, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 393. */
    public static final class Node393<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-393";

        public Node393(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 393, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 394. */
    public static final class Node394<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-394";

        public Node394(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 394, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 395. */
    public static final class Node395<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-395";

        public Node395(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 396. */
    public static final class Node396<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-396";

        public Node396(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 397. */
    public static final class Node397<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-397";

        public Node397(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 397, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 398. */
    public static final class Node398<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-398";

        public Node398(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 398, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 399. */
    public static final class Node399<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-399";

        public Node399(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 399, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 400. */
    public static final class Node400<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-400";

        public Node400(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 401. */
    public static final class Node401<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-401";

        public Node401(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 401, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 402. */
    public static final class Node402<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-402";

        public Node402(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 402, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 402, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 403. */
    public static final class Node403<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-403";

        public Node403(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 403, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 404. */
    public static final class Node404<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-404";

        public Node404(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 404, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 405. */
    public static final class Node405<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-405";

        public Node405(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 406. */
    public static final class Node406<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-406";

        public Node406(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 406, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 406, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 407. */
    public static final class Node407<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-407";

        public Node407(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 407, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 408. */
    public static final class Node408<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-408";

        public Node408(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 409. */
    public static final class Node409<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-409";

        public Node409(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 410. */
    public static final class Node410<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-410";

        public Node410(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 410, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 411. */
    public static final class Node411<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-411";

        public Node411(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 411, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 411, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 412. */
    public static final class Node412<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-412";

        public Node412(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 413. */
    public static final class Node413<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-413";

        public Node413(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 414. */
    public static final class Node414<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-414";

        public Node414(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 414, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 415. */
    public static final class Node415<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-415";

        public Node415(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 415, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 416. */
    public static final class Node416<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-416";

        public Node416(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 416, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 417. */
    public static final class Node417<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-417";

        public Node417(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 418. */
    public static final class Node418<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-418";

        public Node418(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 419. */
    public static final class Node419<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-419";

        public Node419(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 420. */
    public static final class Node420<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-420";

        public Node420(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 420, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 421. */
    public static final class Node421<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-421";

        public Node421(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 421, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 422. */
    public static final class Node422<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-422";

        public Node422(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 423. */
    public static final class Node423<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-423";

        public Node423(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 423, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 424. */
    public static final class Node424<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-424";

        public Node424(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 425. */
    public static final class Node425<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-425";

        public Node425(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 425, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 426. */
    public static final class Node426<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-426";

        public Node426(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 426, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 426, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 427. */
    public static final class Node427<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-427";

        public Node427(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 427, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 428. */
    public static final class Node428<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-428";

        public Node428(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 428, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 428, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 429. */
    public static final class Node429<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-429";

        public Node429(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 429, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 429, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 430. */
    public static final class Node430<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-430";

        public Node430(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 431. */
    public static final class Node431<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-431";

        public Node431(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 432. */
    public static final class Node432<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-432";

        public Node432(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 432, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 433. */
    public static final class Node433<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-433";

        public Node433(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 434. */
    public static final class Node434<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-434";

        public Node434(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 434, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 435. */
    public static final class Node435<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-435";

        public Node435(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 436. */
    public static final class Node436<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-436";

        public Node436(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 437. */
    public static final class Node437<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-437";

        public Node437(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 437, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 438. */
    public static final class Node438<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-438";

        public Node438(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 439. */
    public static final class Node439<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-439";

        public Node439(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 440. */
    public static final class Node440<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-440";

        public Node440(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 440, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 441. */
    public static final class Node441<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-441";

        public Node441(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 441, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 441, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 442. */
    public static final class Node442<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-442";

        public Node442(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 443. */
    public static final class Node443<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-443";

        public Node443(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 444. */
    public static final class Node444<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-444";

        public Node444(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 445. */
    public static final class Node445<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-445";

        public Node445(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 445, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 445, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 445, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 446. */
    public static final class Node446<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-446";

        public Node446(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 447. */
    public static final class Node447<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-447";

        public Node447(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 447, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 448. */
    public static final class Node448<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-448";

        public Node448(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 448, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 449. */
    public static final class Node449<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-449";

        public Node449(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 450. */
    public static final class Node450<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-450";

        public Node450(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 451. */
    public static final class Node451<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-451";

        public Node451(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 452. */
    public static final class Node452<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-452";

        public Node452(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 452, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 453. */
    public static final class Node453<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-453";

        public Node453(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 454. */
    public static final class Node454<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-454";

        public Node454(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 454, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 455. */
    public static final class Node455<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-455";

        public Node455(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 455, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 456. */
    public static final class Node456<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-456";

        public Node456(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 456, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 457. */
    public static final class Node457<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-457";

        public Node457(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 458. */
    public static final class Node458<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-458";

        public Node458(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 458, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 459. */
    public static final class Node459<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-459";

        public Node459(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 460. */
    public static final class Node460<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-460";

        public Node460(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 460, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 461. */
    public static final class Node461<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-461";

        public Node461(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 462. */
    public static final class Node462<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-462";

        public Node462(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 462, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 463. */
    public static final class Node463<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-463";

        public Node463(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 463, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 464. */
    public static final class Node464<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-464";

        public Node464(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 464, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 465. */
    public static final class Node465<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-465";

        public Node465(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 466. */
    public static final class Node466<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-466";

        public Node466(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 466, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 467. */
    public static final class Node467<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-467";

        public Node467(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 468. */
    public static final class Node468<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-468";

        public Node468(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 468, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 468, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 469. */
    public static final class Node469<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-469";

        public Node469(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 470. */
    public static final class Node470<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-470";

        public Node470(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 470, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 471. */
    public static final class Node471<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-471";

        public Node471(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 471, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 472. */
    public static final class Node472<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-472";

        public Node472(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 473. */
    public static final class Node473<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-473";

        public Node473(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 473, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 473, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 473, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 474. */
    public static final class Node474<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-474";

        public Node474(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 475. */
    public static final class Node475<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-475";

        public Node475(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 475, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 476. */
    public static final class Node476<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-476";

        public Node476(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 476, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 477. */
    public static final class Node477<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-477";

        public Node477(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 478. */
    public static final class Node478<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-478";

        public Node478(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 478, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 479. */
    public static final class Node479<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-479";

        public Node479(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 479, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 479, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 480. */
    public static final class Node480<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-480";

        public Node480(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 480, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 480, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 481. */
    public static final class Node481<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-481";

        public Node481(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 482. */
    public static final class Node482<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-482";

        public Node482(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 483. */
    public static final class Node483<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-483";

        public Node483(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 483, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 484. */
    public static final class Node484<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-484";

        public Node484(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 485. */
    public static final class Node485<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-485";

        public Node485(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 486. */
    public static final class Node486<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-486";

        public Node486(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 487. */
    public static final class Node487<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-487";

        public Node487(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 487, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 488. */
    public static final class Node488<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-488";

        public Node488(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 489. */
    public static final class Node489<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-489";

        public Node489(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 490. */
    public static final class Node490<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-490";

        public Node490(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 491. */
    public static final class Node491<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-491";

        public Node491(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 491, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 491, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 492. */
    public static final class Node492<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-492";

        public Node492(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 492, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 492, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 493. */
    public static final class Node493<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-493";

        public Node493(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 493, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 494. */
    public static final class Node494<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-494";

        public Node494(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 494, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 495. */
    public static final class Node495<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-495";

        public Node495(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 496. */
    public static final class Node496<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-496";

        public Node496(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 496, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 497. */
    public static final class Node497<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-497";

        public Node497(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 498. */
    public static final class Node498<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-498";

        public Node498(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 498, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 499. */
    public static final class Node499<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-499";

        public Node499(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 499, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 500. */
    public static final class Node500<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-500";

        public Node500(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 501. */
    public static final class Node501<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-501";

        public Node501(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 502. */
    public static final class Node502<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-502";

        public Node502(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 503. */
    public static final class Node503<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-503";

        public Node503(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 504. */
    public static final class Node504<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-504";

        public Node504(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 504, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 504, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 504, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 505. */
    public static final class Node505<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-505";

        public Node505(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 505, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 506. */
    public static final class Node506<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-506";

        public Node506(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 507. */
    public static final class Node507<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-507";

        public Node507(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 508. */
    public static final class Node508<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-508";

        public Node508(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 509. */
    public static final class Node509<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-509";

        public Node509(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 509, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 509, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 510. */
    public static final class Node510<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-510";

        public Node510(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 511. */
    public static final class Node511<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-511";

        public Node511(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 511, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 512. */
    public static final class Node512<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-512";

        public Node512(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 512, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 513. */
    public static final class Node513<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-513";

        public Node513(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 513, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 514. */
    public static final class Node514<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-514";

        public Node514(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 515. */
    public static final class Node515<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-515";

        public Node515(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 515, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 515, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 516. */
    public static final class Node516<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-516";

        public Node516(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 516, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 517. */
    public static final class Node517<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-517";

        public Node517(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 518. */
    public static final class Node518<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-518";

        public Node518(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 518, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 519. */
    public static final class Node519<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-519";

        public Node519(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 519, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 520. */
    public static final class Node520<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-520";

        public Node520(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 520, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 521. */
    public static final class Node521<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-521";

        public Node521(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 521, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 522. */
    public static final class Node522<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-522";

        public Node522(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 522, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 523. */
    public static final class Node523<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-523";

        public Node523(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 523, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 524. */
    public static final class Node524<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-524";

        public Node524(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 525. */
    public static final class Node525<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-525";

        public Node525(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 526. */
    public static final class Node526<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-526";

        public Node526(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 526, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 526, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 527. */
    public static final class Node527<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-527";

        public Node527(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 528. */
    public static final class Node528<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-528";

        public Node528(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 528, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 529. */
    public static final class Node529<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-529";

        public Node529(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 530. */
    public static final class Node530<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-530";

        public Node530(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 530, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 531. */
    public static final class Node531<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-531";

        public Node531(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 532. */
    public static final class Node532<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-532";

        public Node532(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 533. */
    public static final class Node533<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-533";

        public Node533(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 534. */
    public static final class Node534<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-534";

        public Node534(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 534, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 535. */
    public static final class Node535<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-535";

        public Node535(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 536. */
    public static final class Node536<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-536";

        public Node536(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 536, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 536, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 536, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 537. */
    public static final class Node537<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-537";

        public Node537(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 537, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 537, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 538. */
    public static final class Node538<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-538";

        public Node538(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 538, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 539. */
    public static final class Node539<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-539";

        public Node539(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 539, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 539, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 540. */
    public static final class Node540<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-540";

        public Node540(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 540, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 541. */
    public static final class Node541<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-541";

        public Node541(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 542. */
    public static final class Node542<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-542";

        public Node542(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 543. */
    public static final class Node543<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-543";

        public Node543(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 543, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 544. */
    public static final class Node544<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-544";

        public Node544(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 545. */
    public static final class Node545<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-545";

        public Node545(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 545, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 545, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 546. */
    public static final class Node546<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-546";

        public Node546(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 546, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 547. */
    public static final class Node547<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-547";

        public Node547(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 547, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 547, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 548. */
    public static final class Node548<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-548";

        public Node548(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 549. */
    public static final class Node549<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-549";

        public Node549(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 549, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 549, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 550. */
    public static final class Node550<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-550";

        public Node550(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 550, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 551. */
    public static final class Node551<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-551";

        public Node551(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 552. */
    public static final class Node552<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-552";

        public Node552(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 552, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 553. */
    public static final class Node553<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-553";

        public Node553(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 553, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 554. */
    public static final class Node554<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-554";

        public Node554(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 555. */
    public static final class Node555<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-555";

        public Node555(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 556. */
    public static final class Node556<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-556";

        public Node556(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 557. */
    public static final class Node557<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-557";

        public Node557(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 557, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 557, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 558. */
    public static final class Node558<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-558";

        public Node558(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 558, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 559. */
    public static final class Node559<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-559";

        public Node559(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 559, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 560. */
    public static final class Node560<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-560";

        public Node560(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 560, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 561. */
    public static final class Node561<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-561";

        public Node561(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 562. */
    public static final class Node562<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-562";

        public Node562(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 562, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 563. */
    public static final class Node563<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-563";

        public Node563(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 564. */
    public static final class Node564<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-564";

        public Node564(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 564, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 565. */
    public static final class Node565<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-565";

        public Node565(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 566. */
    public static final class Node566<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-566";

        public Node566(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 567. */
    public static final class Node567<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-567";

        public Node567(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 568. */
    public static final class Node568<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-568";

        public Node568(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 569. */
    public static final class Node569<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-569";

        public Node569(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 570. */
    public static final class Node570<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-570";

        public Node570(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 571. */
    public static final class Node571<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-571";

        public Node571(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 571, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 571, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 572. */
    public static final class Node572<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-572";

        public Node572(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 572, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 572, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 572, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 572, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 573. */
    public static final class Node573<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-573";

        public Node573(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 573, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 574. */
    public static final class Node574<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-574";

        public Node574(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 574, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 574, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 575. */
    public static final class Node575<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-575";

        public Node575(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 575, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 576. */
    public static final class Node576<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-576";

        public Node576(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 576, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 577. */
    public static final class Node577<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-577";

        public Node577(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 577, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 577, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 578. */
    public static final class Node578<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-578";

        public Node578(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 579. */
    public static final class Node579<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-579";

        public Node579(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 580. */
    public static final class Node580<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-580";

        public Node580(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 580, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 581. */
    public static final class Node581<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-581";

        public Node581(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 582. */
    public static final class Node582<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-582";

        public Node582(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 582, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 583. */
    public static final class Node583<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-583";

        public Node583(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 583, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 584. */
    public static final class Node584<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-584";

        public Node584(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 584, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 584, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 585. */
    public static final class Node585<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-585";

        public Node585(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 585, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 586. */
    public static final class Node586<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-586";

        public Node586(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 586, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 586, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 587. */
    public static final class Node587<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-587";

        public Node587(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 588. */
    public static final class Node588<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-588";

        public Node588(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 588, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 589. */
    public static final class Node589<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-589";

        public Node589(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 589, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 589, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 590. */
    public static final class Node590<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-590";

        public Node590(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 590, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 591. */
    public static final class Node591<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-591";

        public Node591(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 592. */
    public static final class Node592<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-592";

        public Node592(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 593. */
    public static final class Node593<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-593";

        public Node593(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 593, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 594. */
    public static final class Node594<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-594";

        public Node594(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 595. */
    public static final class Node595<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-595";

        public Node595(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 595, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 596. */
    public static final class Node596<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-596";

        public Node596(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 597. */
    public static final class Node597<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-597";

        public Node597(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 598. */
    public static final class Node598<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-598";

        public Node598(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 599. */
    public static final class Node599<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-599";

        public Node599(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 599, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 600. */
    public static final class Node600<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-600";

        public Node600(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 600, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 601. */
    public static final class Node601<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-601";

        public Node601(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 602. */
    public static final class Node602<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-602";

        public Node602(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 602, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 603. */
    public static final class Node603<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-603";

        public Node603(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 603, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 604. */
    public static final class Node604<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-604";

        public Node604(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 604, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 604, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 604, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 605. */
    public static final class Node605<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-605";

        public Node605(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 605, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 605, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 606. */
    public static final class Node606<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-606";

        public Node606(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 606, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 607. */
    public static final class Node607<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-607";

        public Node607(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 608. */
    public static final class Node608<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-608";

        public Node608(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 609. */
    public static final class Node609<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-609";

        public Node609(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 609, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 610. */
    public static final class Node610<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-610";

        public Node610(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 610, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 611. */
    public static final class Node611<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-611";

        public Node611(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 612. */
    public static final class Node612<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-612";

        public Node612(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 612, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 613. */
    public static final class Node613<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-613";

        public Node613(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 614. */
    public static final class Node614<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-614";

        public Node614(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 614, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 614, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 615. */
    public static final class Node615<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-615";

        public Node615(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 616. */
    public static final class Node616<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-616";

        public Node616(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 616, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 616, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 616, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 617. */
    public static final class Node617<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-617";

        public Node617(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 617, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 618. */
    public static final class Node618<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-618";

        public Node618(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 618, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 618, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 618, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 619. */
    public static final class Node619<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-619";

        public Node619(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 620. */
    public static final class Node620<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-620";

        public Node620(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 621. */
    public static final class Node621<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-621";

        public Node621(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 622. */
    public static final class Node622<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-622";

        public Node622(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 623. */
    public static final class Node623<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-623";

        public Node623(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 624. */
    public static final class Node624<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-624";

        public Node624(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 625. */
    public static final class Node625<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-625";

        public Node625(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 626. */
    public static final class Node626<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-626";

        public Node626(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 626, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 627. */
    public static final class Node627<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-627";

        public Node627(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 628. */
    public static final class Node628<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-628";

        public Node628(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 629. */
    public static final class Node629<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-629";

        public Node629(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 629, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 630. */
    public static final class Node630<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-630";

        public Node630(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 631. */
    public static final class Node631<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-631";

        public Node631(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 632. */
    public static final class Node632<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-632";

        public Node632(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 632, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 633. */
    public static final class Node633<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-633";

        public Node633(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 633, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 634. */
    public static final class Node634<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-634";

        public Node634(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 634, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 635. */
    public static final class Node635<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-635";

        public Node635(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 636. */
    public static final class Node636<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-636";

        public Node636(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 636, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 637. */
    public static final class Node637<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-637";

        public Node637(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 638. */
    public static final class Node638<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-638";

        public Node638(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 639. */
    public static final class Node639<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-639";

        public Node639(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 639, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 640. */
    public static final class Node640<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-640";

        public Node640(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 640, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 641. */
    public static final class Node641<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-641";

        public Node641(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 642. */
    public static final class Node642<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-642";

        public Node642(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 643. */
    public static final class Node643<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-643";

        public Node643(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 643, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 643, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 644. */
    public static final class Node644<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-644";

        public Node644(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 645. */
    public static final class Node645<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-645";

        public Node645(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 646. */
    public static final class Node646<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-646";

        public Node646(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 646, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 646, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 647. */
    public static final class Node647<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-647";

        public Node647(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 647, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 648. */
    public static final class Node648<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-648";

        public Node648(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 648, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 649. */
    public static final class Node649<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-649";

        public Node649(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 649, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 650. */
    public static final class Node650<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-650";

        public Node650(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 650, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 650, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 651. */
    public static final class Node651<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-651";

        public Node651(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 652. */
    public static final class Node652<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-652";

        public Node652(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 653. */
    public static final class Node653<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-653";

        public Node653(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 653, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 653, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 654. */
    public static final class Node654<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-654";

        public Node654(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 654, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 655. */
    public static final class Node655<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-655";

        public Node655(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 655, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 656. */
    public static final class Node656<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-656";

        public Node656(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 656, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 657. */
    public static final class Node657<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-657";

        public Node657(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 658. */
    public static final class Node658<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-658";

        public Node658(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 658, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 658, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 659. */
    public static final class Node659<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-659";

        public Node659(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 660. */
    public static final class Node660<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-660";

        public Node660(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 660, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 661. */
    public static final class Node661<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-661";

        public Node661(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 662. */
    public static final class Node662<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-662";

        public Node662(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 662, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 662, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 663. */
    public static final class Node663<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-663";

        public Node663(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 663, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 664. */
    public static final class Node664<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-664";

        public Node664(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 664, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 665. */
    public static final class Node665<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-665";

        public Node665(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 665, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 665, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 665, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 666. */
    public static final class Node666<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-666";

        public Node666(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 666, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 667. */
    public static final class Node667<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-667";

        public Node667(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 667, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 668. */
    public static final class Node668<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-668";

        public Node668(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 668, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 668, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 669. */
    public static final class Node669<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-669";

        public Node669(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 669, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 670. */
    public static final class Node670<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-670";

        public Node670(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 670, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 670, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 671. */
    public static final class Node671<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-671";

        public Node671(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 671, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 671, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 671, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 672. */
    public static final class Node672<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-672";

        public Node672(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 672, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 673. */
    public static final class Node673<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-673";

        public Node673(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 673, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 674. */
    public static final class Node674<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-674";

        public Node674(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 675. */
    public static final class Node675<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-675";

        public Node675(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 676. */
    public static final class Node676<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-676";

        public Node676(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 676, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 677. */
    public static final class Node677<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-677";

        public Node677(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 677, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 678. */
    public static final class Node678<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-678";

        public Node678(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 678, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 679. */
    public static final class Node679<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-679";

        public Node679(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 679, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 679, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 680. */
    public static final class Node680<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-680";

        public Node680(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 680, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 681. */
    public static final class Node681<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-681";

        public Node681(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 682. */
    public static final class Node682<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-682";

        public Node682(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 682, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 683. */
    public static final class Node683<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-683";

        public Node683(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 683, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 684. */
    public static final class Node684<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-684";

        public Node684(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 685. */
    public static final class Node685<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-685";

        public Node685(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 686. */
    public static final class Node686<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-686";

        public Node686(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 686, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 687. */
    public static final class Node687<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-687";

        public Node687(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 688. */
    public static final class Node688<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-688";

        public Node688(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 689. */
    public static final class Node689<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-689";

        public Node689(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 690. */
    public static final class Node690<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-690";

        public Node690(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 690, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 690, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 691. */
    public static final class Node691<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-691";

        public Node691(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 691, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 692. */
    public static final class Node692<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-692";

        public Node692(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 693. */
    public static final class Node693<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-693";

        public Node693(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 693, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 694. */
    public static final class Node694<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-694";

        public Node694(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 694, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 695. */
    public static final class Node695<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-695";

        public Node695(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 696. */
    public static final class Node696<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-696";

        public Node696(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 696, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 697. */
    public static final class Node697<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-697";

        public Node697(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 698. */
    public static final class Node698<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-698";

        public Node698(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 699. */
    public static final class Node699<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-699";

        public Node699(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 699, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 699, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 700. */
    public static final class Node700<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-700";

        public Node700(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 700, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 701. */
    public static final class Node701<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-701";

        public Node701(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 701, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 701, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 702. */
    public static final class Node702<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-702";

        public Node702(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 703. */
    public static final class Node703<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-703";

        public Node703(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 704. */
    public static final class Node704<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-704";

        public Node704(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 704, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 704, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 705. */
    public static final class Node705<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-705";

        public Node705(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 706. */
    public static final class Node706<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-706";

        public Node706(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 706, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 707. */
    public static final class Node707<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-707";

        public Node707(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 707, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 708. */
    public static final class Node708<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-708";

        public Node708(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 708, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 709. */
    public static final class Node709<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-709";

        public Node709(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 709, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 710. */
    public static final class Node710<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-710";

        public Node710(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 710, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 710, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 711. */
    public static final class Node711<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-711";

        public Node711(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 711, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 712. */
    public static final class Node712<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-712";

        public Node712(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 712, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 713. */
    public static final class Node713<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-713";

        public Node713(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 714. */
    public static final class Node714<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-714";

        public Node714(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 714, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 715. */
    public static final class Node715<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-715";

        public Node715(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 715, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 716. */
    public static final class Node716<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-716";

        public Node716(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 716, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 716, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 717. */
    public static final class Node717<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-717";

        public Node717(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 717, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 718. */
    public static final class Node718<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-718";

        public Node718(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 718, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 719. */
    public static final class Node719<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-719";

        public Node719(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 720. */
    public static final class Node720<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-720";

        public Node720(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 721. */
    public static final class Node721<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-721";

        public Node721(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 721, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 721, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 722. */
    public static final class Node722<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-722";

        public Node722(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 722, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 723. */
    public static final class Node723<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-723";

        public Node723(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 724. */
    public static final class Node724<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-724";

        public Node724(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 725. */
    public static final class Node725<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-725";

        public Node725(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 726. */
    public static final class Node726<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-726";

        public Node726(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 727. */
    public static final class Node727<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-727";

        public Node727(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 728. */
    public static final class Node728<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-728";

        public Node728(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 729. */
    public static final class Node729<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-729";

        public Node729(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 729, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 729, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 730. */
    public static final class Node730<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-730";

        public Node730(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 731. */
    public static final class Node731<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-731";

        public Node731(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 731, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 731, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 732. */
    public static final class Node732<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-732";

        public Node732(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 732, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 733. */
    public static final class Node733<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-733";

        public Node733(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 733, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 734. */
    public static final class Node734<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-734";

        public Node734(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 734, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 735. */
    public static final class Node735<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-735";

        public Node735(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 735, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 736. */
    public static final class Node736<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-736";

        public Node736(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 737. */
    public static final class Node737<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-737";

        public Node737(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 738. */
    public static final class Node738<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-738";

        public Node738(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 739. */
    public static final class Node739<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-739";

        public Node739(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 740. */
    public static final class Node740<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-740";

        public Node740(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 741. */
    public static final class Node741<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-741";

        public Node741(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 741, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 741, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 742. */
    public static final class Node742<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-742";

        public Node742(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 743. */
    public static final class Node743<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-743";

        public Node743(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 744. */
    public static final class Node744<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-744";

        public Node744(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 744, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 745. */
    public static final class Node745<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-745";

        public Node745(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 745, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 745, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 746. */
    public static final class Node746<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-746";

        public Node746(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 746, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 747. */
    public static final class Node747<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-747";

        public Node747(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 748. */
    public static final class Node748<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-748";

        public Node748(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 748, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 749. */
    public static final class Node749<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-749";

        public Node749(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 749, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 750. */
    public static final class Node750<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-750";

        public Node750(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 751. */
    public static final class Node751<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-751";

        public Node751(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 752. */
    public static final class Node752<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-752";

        public Node752(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 752, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 752, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 753. */
    public static final class Node753<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-753";

        public Node753(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 754. */
    public static final class Node754<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-754";

        public Node754(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 754, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 755. */
    public static final class Node755<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-755";

        public Node755(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 756. */
    public static final class Node756<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-756";

        public Node756(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 757. */
    public static final class Node757<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-757";

        public Node757(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 757, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 758. */
    public static final class Node758<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-758";

        public Node758(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 758, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 759. */
    public static final class Node759<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-759";

        public Node759(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 760. */
    public static final class Node760<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-760";

        public Node760(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 761. */
    public static final class Node761<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-761";

        public Node761(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 762. */
    public static final class Node762<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-762";

        public Node762(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 763. */
    public static final class Node763<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-763";

        public Node763(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 764. */
    public static final class Node764<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-764";

        public Node764(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 765. */
    public static final class Node765<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-765";

        public Node765(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 765, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 766. */
    public static final class Node766<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-766";

        public Node766(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 767. */
    public static final class Node767<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-767";

        public Node767(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 768. */
    public static final class Node768<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-768";

        public Node768(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 768, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 769. */
    public static final class Node769<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-769";

        public Node769(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 770. */
    public static final class Node770<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-770";

        public Node770(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 770, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 770, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 771. */
    public static final class Node771<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-771";

        public Node771(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 771, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 772. */
    public static final class Node772<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-772";

        public Node772(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 773. */
    public static final class Node773<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-773";

        public Node773(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 773, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 774. */
    public static final class Node774<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-774";

        public Node774(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 774, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 774, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 775. */
    public static final class Node775<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-775";

        public Node775(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 775, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 776. */
    public static final class Node776<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-776";

        public Node776(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 776, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 777. */
    public static final class Node777<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-777";

        public Node777(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 778. */
    public static final class Node778<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-778";

        public Node778(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 778, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 779. */
    public static final class Node779<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-779";

        public Node779(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 779, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 780. */
    public static final class Node780<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-780";

        public Node780(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 780, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 781. */
    public static final class Node781<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-781";

        public Node781(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 781, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 781, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 782. */
    public static final class Node782<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-782";

        public Node782(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 782, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 783. */
    public static final class Node783<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-783";

        public Node783(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 783, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 783, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 784. */
    public static final class Node784<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-784";

        public Node784(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 784, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 784, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 784, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 785. */
    public static final class Node785<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-785";

        public Node785(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 785, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 786. */
    public static final class Node786<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-786";

        public Node786(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 786, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 786, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 787. */
    public static final class Node787<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-787";

        public Node787(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 787, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 787, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 788. */
    public static final class Node788<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-788";

        public Node788(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 788, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 788, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 789. */
    public static final class Node789<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-789";

        public Node789(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 790. */
    public static final class Node790<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-790";

        public Node790(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 791. */
    public static final class Node791<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-791";

        public Node791(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 792. */
    public static final class Node792<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-792";

        public Node792(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 792, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 793. */
    public static final class Node793<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-793";

        public Node793(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 794. */
    public static final class Node794<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-794";

        public Node794(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 795. */
    public static final class Node795<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-795";

        public Node795(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 796. */
    public static final class Node796<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-796";

        public Node796(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 796, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 796, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 797. */
    public static final class Node797<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-797";

        public Node797(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 797, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 798. */
    public static final class Node798<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-798";

        public Node798(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 799. */
    public static final class Node799<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-799";

        public Node799(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 799, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 800. */
    public static final class Node800<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-800";

        public Node800(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 801. */
    public static final class Node801<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-801";

        public Node801(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 801, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 802. */
    public static final class Node802<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-802";

        public Node802(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 803. */
    public static final class Node803<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-803";

        public Node803(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 803, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 804. */
    public static final class Node804<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-804";

        public Node804(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 804, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 804, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 805. */
    public static final class Node805<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-805";

        public Node805(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 806. */
    public static final class Node806<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-806";

        public Node806(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 807. */
    public static final class Node807<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-807";

        public Node807(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 807, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 808. */
    public static final class Node808<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-808";

        public Node808(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 808, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 809. */
    public static final class Node809<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-809";

        public Node809(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 809, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 810. */
    public static final class Node810<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-810";

        public Node810(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 810, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 811. */
    public static final class Node811<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-811";

        public Node811(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 812. */
    public static final class Node812<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-812";

        public Node812(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 812, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 812, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 813. */
    public static final class Node813<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-813";

        public Node813(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 813, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 814. */
    public static final class Node814<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-814";

        public Node814(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 814, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 814, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 815. */
    public static final class Node815<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-815";

        public Node815(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 815, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 816. */
    public static final class Node816<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-816";

        public Node816(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 817. */
    public static final class Node817<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-817";

        public Node817(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 818. */
    public static final class Node818<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-818";

        public Node818(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 819. */
    public static final class Node819<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-819";

        public Node819(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 820. */
    public static final class Node820<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-820";

        public Node820(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 821. */
    public static final class Node821<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-821";

        public Node821(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 821, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 822. */
    public static final class Node822<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-822";

        public Node822(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 823. */
    public static final class Node823<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-823";

        public Node823(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 823, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 824. */
    public static final class Node824<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-824";

        public Node824(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 824, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 825. */
    public static final class Node825<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-825";

        public Node825(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 826. */
    public static final class Node826<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-826";

        public Node826(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 827. */
    public static final class Node827<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-827";

        public Node827(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 827, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 828. */
    public static final class Node828<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-828";

        public Node828(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 828, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 829. */
    public static final class Node829<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-829";

        public Node829(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 829, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 830. */
    public static final class Node830<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-830";

        public Node830(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 830, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 831. */
    public static final class Node831<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-831";

        public Node831(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 832. */
    public static final class Node832<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-832";

        public Node832(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 833. */
    public static final class Node833<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-833";

        public Node833(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 833, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 833, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 834. */
    public static final class Node834<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-834";

        public Node834(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 835. */
    public static final class Node835<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-835";

        public Node835(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 836. */
    public static final class Node836<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-836";

        public Node836(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 837. */
    public static final class Node837<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-837";

        public Node837(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 837, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 838. */
    public static final class Node838<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-838";

        public Node838(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 838, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 838, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 838, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 839. */
    public static final class Node839<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-839";

        public Node839(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 840. */
    public static final class Node840<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-840";

        public Node840(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 841. */
    public static final class Node841<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-841";

        public Node841(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 841, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 841, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 842. */
    public static final class Node842<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-842";

        public Node842(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 842, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 842, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 843. */
    public static final class Node843<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-843";

        public Node843(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 843, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 844. */
    public static final class Node844<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-844";

        public Node844(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 844, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 845. */
    public static final class Node845<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-845";

        public Node845(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 845, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 845, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 845, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 846. */
    public static final class Node846<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-846";

        public Node846(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 847. */
    public static final class Node847<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-847";

        public Node847(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 847, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 847, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 848. */
    public static final class Node848<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-848";

        public Node848(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 849. */
    public static final class Node849<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-849";

        public Node849(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 849, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 849, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 850. */
    public static final class Node850<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-850";

        public Node850(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 850, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 850, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 851. */
    public static final class Node851<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-851";

        public Node851(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 852. */
    public static final class Node852<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-852";

        public Node852(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 852, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 852, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 852, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 853. */
    public static final class Node853<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-853";

        public Node853(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 853, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 853, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 854. */
    public static final class Node854<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-854";

        public Node854(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 854, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 855. */
    public static final class Node855<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-855";

        public Node855(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 855, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 855, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 856. */
    public static final class Node856<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-856";

        public Node856(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 856, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 857. */
    public static final class Node857<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-857";

        public Node857(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 857, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 857, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 858. */
    public static final class Node858<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-858";

        public Node858(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 859. */
    public static final class Node859<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-859";

        public Node859(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 859, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 860. */
    public static final class Node860<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-860";

        public Node860(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 861. */
    public static final class Node861<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-861";

        public Node861(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 861, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 862. */
    public static final class Node862<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-862";

        public Node862(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 862, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 863. */
    public static final class Node863<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-863";

        public Node863(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 864. */
    public static final class Node864<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-864";

        public Node864(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 865. */
    public static final class Node865<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-865";

        public Node865(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 865, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 865, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 866. */
    public static final class Node866<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-866";

        public Node866(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 866, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 867. */
    public static final class Node867<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-867";

        public Node867(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 868. */
    public static final class Node868<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-868";

        public Node868(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 868, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 868, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 869. */
    public static final class Node869<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-869";

        public Node869(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 870. */
    public static final class Node870<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-870";

        public Node870(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 870, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 870, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 871. */
    public static final class Node871<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-871";

        public Node871(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 871, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 872. */
    public static final class Node872<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-872";

        public Node872(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 873. */
    public static final class Node873<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-873";

        public Node873(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 873, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 874. */
    public static final class Node874<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-874";

        public Node874(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 875. */
    public static final class Node875<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-875";

        public Node875(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 875, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 876. */
    public static final class Node876<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-876";

        public Node876(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 877. */
    public static final class Node877<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-877";

        public Node877(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 878. */
    public static final class Node878<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-878";

        public Node878(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 878, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 879. */
    public static final class Node879<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-879";

        public Node879(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 879, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 879, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 880. */
    public static final class Node880<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-880";

        public Node880(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 881. */
    public static final class Node881<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-881";

        public Node881(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 882. */
    public static final class Node882<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-882";

        public Node882(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 882, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 882, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 883. */
    public static final class Node883<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-883";

        public Node883(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 883, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 884. */
    public static final class Node884<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-884";

        public Node884(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 884, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 884, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 885. */
    public static final class Node885<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-885";

        public Node885(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 886. */
    public static final class Node886<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-886";

        public Node886(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 886, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 887. */
    public static final class Node887<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-887";

        public Node887(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 887, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 888. */
    public static final class Node888<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-888";

        public Node888(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 889. */
    public static final class Node889<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-889";

        public Node889(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 889, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 890. */
    public static final class Node890<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-890";

        public Node890(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 890, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 891. */
    public static final class Node891<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-891";

        public Node891(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 892. */
    public static final class Node892<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-892";

        public Node892(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 893. */
    public static final class Node893<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-893";

        public Node893(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 893, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 894. */
    public static final class Node894<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-894";

        public Node894(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 894, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 895. */
    public static final class Node895<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-895";

        public Node895(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 895, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 896. */
    public static final class Node896<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-896";

        public Node896(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 896, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 896, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 897. */
    public static final class Node897<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-897";

        public Node897(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 897, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 897, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 898. */
    public static final class Node898<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-898";

        public Node898(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 899. */
    public static final class Node899<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-899";

        public Node899(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 900. */
    public static final class Node900<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-900";

        public Node900(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 900, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 901. */
    public static final class Node901<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-901";

        public Node901(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 901, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 902. */
    public static final class Node902<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-902";

        public Node902(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 903. */
    public static final class Node903<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-903";

        public Node903(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 904. */
    public static final class Node904<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-904";

        public Node904(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 905. */
    public static final class Node905<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-905";

        public Node905(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 905, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 906. */
    public static final class Node906<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-906";

        public Node906(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 906, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 907. */
    public static final class Node907<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-907";

        public Node907(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 907, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 908. */
    public static final class Node908<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-908";

        public Node908(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 909. */
    public static final class Node909<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-909";

        public Node909(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 910. */
    public static final class Node910<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-910";

        public Node910(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 911. */
    public static final class Node911<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-911";

        public Node911(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 911, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 912. */
    public static final class Node912<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-912";

        public Node912(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 912, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 913. */
    public static final class Node913<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-913";

        public Node913(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 914. */
    public static final class Node914<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-914";

        public Node914(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 914, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 915. */
    public static final class Node915<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-915";

        public Node915(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 915, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 916. */
    public static final class Node916<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-916";

        public Node916(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 917. */
    public static final class Node917<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-917";

        public Node917(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 917, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 918. */
    public static final class Node918<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-918";

        public Node918(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 918, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 919. */
    public static final class Node919<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-919";

        public Node919(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 919, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 920. */
    public static final class Node920<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-920";

        public Node920(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 920, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 921. */
    public static final class Node921<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-921";

        public Node921(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 922. */
    public static final class Node922<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-922";

        public Node922(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 923. */
    public static final class Node923<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-923";

        public Node923(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 923, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 924. */
    public static final class Node924<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-924";

        public Node924(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 924, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 925. */
    public static final class Node925<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-925";

        public Node925(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 925, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 925, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 926. */
    public static final class Node926<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-926";

        public Node926(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 926, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 926, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 927. */
    public static final class Node927<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-927";

        public Node927(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 928. */
    public static final class Node928<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-928";

        public Node928(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 928, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 928, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 928, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 929. */
    public static final class Node929<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-929";

        public Node929(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 929, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 930. */
    public static final class Node930<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-930";

        public Node930(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 930, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 931. */
    public static final class Node931<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-931";

        public Node931(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 932. */
    public static final class Node932<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-932";

        public Node932(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 932, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 932, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 933. */
    public static final class Node933<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-933";

        public Node933(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 933, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 934. */
    public static final class Node934<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-934";

        public Node934(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 934, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 935. */
    public static final class Node935<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-935";

        public Node935(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 936. */
    public static final class Node936<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-936";

        public Node936(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 936, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 937. */
    public static final class Node937<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-937";

        public Node937(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 937, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 938. */
    public static final class Node938<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-938";

        public Node938(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 938, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 938, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 939. */
    public static final class Node939<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-939";

        public Node939(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 940. */
    public static final class Node940<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-940";

        public Node940(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 941. */
    public static final class Node941<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-941";

        public Node941(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 941, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 941, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 942. */
    public static final class Node942<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-942";

        public Node942(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 943. */
    public static final class Node943<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-943";

        public Node943(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 944. */
    public static final class Node944<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-944";

        public Node944(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 945. */
    public static final class Node945<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-945";

        public Node945(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 945, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 946. */
    public static final class Node946<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-946";

        public Node946(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 946, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 947. */
    public static final class Node947<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-947";

        public Node947(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 948. */
    public static final class Node948<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-948";

        public Node948(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 948, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 949. */
    public static final class Node949<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-949";

        public Node949(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 950. */
    public static final class Node950<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-950";

        public Node950(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 951. */
    public static final class Node951<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-951";

        public Node951(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 952. */
    public static final class Node952<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-952";

        public Node952(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 953. */
    public static final class Node953<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-953";

        public Node953(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 953, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 954. */
    public static final class Node954<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-954";

        public Node954(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 955. */
    public static final class Node955<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-955";

        public Node955(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 955, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 956. */
    public static final class Node956<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-956";

        public Node956(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 957. */
    public static final class Node957<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-957";

        public Node957(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 957, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 958. */
    public static final class Node958<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-958";

        public Node958(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 959. */
    public static final class Node959<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-959";

        public Node959(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 960. */
    public static final class Node960<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-960";

        public Node960(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 960, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 960, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 961. */
    public static final class Node961<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-961";

        public Node961(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 962. */
    public static final class Node962<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-962";

        public Node962(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 962, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 963. */
    public static final class Node963<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-963";

        public Node963(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 963, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 963, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 964. */
    public static final class Node964<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-964";

        public Node964(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 965. */
    public static final class Node965<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-965";

        public Node965(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 966. */
    public static final class Node966<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-966";

        public Node966(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 967. */
    public static final class Node967<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-967";

        public Node967(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 967, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 968. */
    public static final class Node968<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-968";

        public Node968(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 968, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 969. */
    public static final class Node969<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-969";

        public Node969(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 970. */
    public static final class Node970<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-970";

        public Node970(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 971. */
    public static final class Node971<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-971";

        public Node971(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 971, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 972. */
    public static final class Node972<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-972";

        public Node972(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 973. */
    public static final class Node973<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-973";

        public Node973(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 973, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 974. */
    public static final class Node974<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-974";

        public Node974(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 974, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 975. */
    public static final class Node975<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-975";

        public Node975(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 976. */
    public static final class Node976<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-976";

        public Node976(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 977. */
    public static final class Node977<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-977";

        public Node977(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 978. */
    public static final class Node978<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-978";

        public Node978(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 978, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 979. */
    public static final class Node979<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-979";

        public Node979(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 980. */
    public static final class Node980<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-980";

        public Node980(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 981. */
    public static final class Node981<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-981";

        public Node981(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 981, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 982. */
    public static final class Node982<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-982";

        public Node982(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 982, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 983. */
    public static final class Node983<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-983";

        public Node983(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 983, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 984. */
    public static final class Node984<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-984";

        public Node984(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 984, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 984, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 985. */
    public static final class Node985<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-985";

        public Node985(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 986. */
    public static final class Node986<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-986";

        public Node986(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 986, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 987. */
    public static final class Node987<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-987";

        public Node987(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 987, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 987, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 988. */
    public static final class Node988<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-988";

        public Node988(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 988, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 988, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 989. */
    public static final class Node989<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-989";

        public Node989(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 990. */
    public static final class Node990<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-990";

        public Node990(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 991. */
    public static final class Node991<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-991";

        public Node991(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 991, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 992. */
    public static final class Node992<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-992";

        public Node992(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 992, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 992, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 992, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 993. */
    public static final class Node993<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-993";

        public Node993(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 994. */
    public static final class Node994<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-994";

        public Node994(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 994, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 994, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 995. */
    public static final class Node995<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-995";

        public Node995(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 996. */
    public static final class Node996<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-996";

        public Node996(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 996, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 997. */
    public static final class Node997<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-997";

        public Node997(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 997, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 998. */
    public static final class Node998<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-998";

        public Node998(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 998, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 999. */
    public static final class Node999<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-999";

        public Node999(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 999, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 999, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1000. */
    public static final class Node1000<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1000";

        public Node1000(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1001. */
    public static final class Node1001<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1001";

        public Node1001(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1001, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1002. */
    public static final class Node1002<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1002";

        public Node1002(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1002, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1003. */
    public static final class Node1003<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1003";

        public Node1003(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1004. */
    public static final class Node1004<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1004";

        public Node1004(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1004, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1005. */
    public static final class Node1005<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1005";

        public Node1005(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1005, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 1005, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1006. */
    public static final class Node1006<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1006";

        public Node1006(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1007. */
    public static final class Node1007<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1007";

        public Node1007(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1007, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1008. */
    public static final class Node1008<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1008";

        public Node1008(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1008, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1009. */
    public static final class Node1009<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1009";

        public Node1009(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1009, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1010. */
    public static final class Node1010<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1010";

        public Node1010(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1010, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1011. */
    public static final class Node1011<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1011";

        public Node1011(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1011, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1012. */
    public static final class Node1012<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1012";

        public Node1012(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1012, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1013. */
    public static final class Node1013<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1013";

        public Node1013(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1014. */
    public static final class Node1014<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1014";

        public Node1014(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1015. */
    public static final class Node1015<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1015";

        public Node1015(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1016. */
    public static final class Node1016<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1016";

        public Node1016(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1016, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1017. */
    public static final class Node1017<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1017";

        public Node1017(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1018. */
    public static final class Node1018<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1018";

        public Node1018(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1019. */
    public static final class Node1019<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1019";

        public Node1019(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1019, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1020. */
    public static final class Node1020<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1020";

        public Node1020(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1020, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1021. */
    public static final class Node1021<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1021";

        public Node1021(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1021, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1022. */
    public static final class Node1022<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1022";

        public Node1022(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1023. */
    public static final class Node1023<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1023";

        public Node1023(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1023, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1024. */
    public static final class Node1024<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1024";

        public Node1024(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1024, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1025. */
    public static final class Node1025<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1025";

        public Node1025(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1025, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1026. */
    public static final class Node1026<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1026";

        public Node1026(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1026, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1027. */
    public static final class Node1027<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1027";

        public Node1027(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1027, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1027, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1028. */
    public static final class Node1028<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1028";

        public Node1028(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1029. */
    public static final class Node1029<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1029";

        public Node1029(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1029, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1029, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1030. */
    public static final class Node1030<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1030";

        public Node1030(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1031. */
    public static final class Node1031<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1031";

        public Node1031(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1031, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1032. */
    public static final class Node1032<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1032";

        public Node1032(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1032, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1033. */
    public static final class Node1033<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1033";

        public Node1033(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1034. */
    public static final class Node1034<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1034";

        public Node1034(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1034, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1035. */
    public static final class Node1035<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1035";

        public Node1035(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1036. */
    public static final class Node1036<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1036";

        public Node1036(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1036, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1036, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 1036, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1037. */
    public static final class Node1037<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1037";

        public Node1037(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1037, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1037, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1038. */
    public static final class Node1038<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1038";

        public Node1038(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1039. */
    public static final class Node1039<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1039";

        public Node1039(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1039, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 1039, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1040. */
    public static final class Node1040<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1040";

        public Node1040(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1040, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1041. */
    public static final class Node1041<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1041";

        public Node1041(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1042. */
    public static final class Node1042<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1042";

        public Node1042(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1042, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1043. */
    public static final class Node1043<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1043";

        public Node1043(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1044. */
    public static final class Node1044<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1044";

        public Node1044(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1044, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1045. */
    public static final class Node1045<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1045";

        public Node1045(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1045, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1046. */
    public static final class Node1046<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1046";

        public Node1046(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1046, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1047. */
    public static final class Node1047<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1047";

        public Node1047(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1048. */
    public static final class Node1048<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1048";

        public Node1048(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1048, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1049. */
    public static final class Node1049<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1049";

        public Node1049(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 1049, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1050. */
    public static final class Node1050<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1050";

        public Node1050(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1050, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1051. */
    public static final class Node1051<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1051";

        public Node1051(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1052. */
    public static final class Node1052<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1052";

        public Node1052(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1052, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1053. */
    public static final class Node1053<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1053";

        public Node1053(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1053, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1054. */
    public static final class Node1054<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1054";

        public Node1054(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1055. */
    public static final class Node1055<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1055";

        public Node1055(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1055, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 1055, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1056. */
    public static final class Node1056<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1056";

        public Node1056(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1056, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1057. */
    public static final class Node1057<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1057";

        public Node1057(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1057, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 1057, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1058. */
    public static final class Node1058<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1058";

        public Node1058(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1059. */
    public static final class Node1059<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1059";

        public Node1059(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1060. */
    public static final class Node1060<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1060";

        public Node1060(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1061. */
    public static final class Node1061<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1061";

        public Node1061(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1062. */
    public static final class Node1062<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1062";

        public Node1062(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1063. */
    public static final class Node1063<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1063";

        public Node1063(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1063, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1063, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1064. */
    public static final class Node1064<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1064";

        public Node1064(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 1064, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1065. */
    public static final class Node1065<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1065";

        public Node1065(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1065, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1065, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1066. */
    public static final class Node1066<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1066";

        public Node1066(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1066, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1067. */
    public static final class Node1067<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1067";

        public Node1067(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1068. */
    public static final class Node1068<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1068";

        public Node1068(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1068, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1068, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1069. */
    public static final class Node1069<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1069";

        public Node1069(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1069, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 1069, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1070. */
    public static final class Node1070<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1070";

        public Node1070(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1070, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1070, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1071. */
    public static final class Node1071<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1071";

        public Node1071(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1072. */
    public static final class Node1072<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1072";

        public Node1072(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1073. */
    public static final class Node1073<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1073";

        public Node1073(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1073, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1073, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1074. */
    public static final class Node1074<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1074";

        public Node1074(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1075. */
    public static final class Node1075<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1075";

        public Node1075(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1076. */
    public static final class Node1076<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1076";

        public Node1076(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1076, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 1076, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1077. */
    public static final class Node1077<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1077";

        public Node1077(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1078. */
    public static final class Node1078<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1078";

        public Node1078(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1079. */
    public static final class Node1079<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1079";

        public Node1079(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1079, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1080. */
    public static final class Node1080<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1080";

        public Node1080(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1081. */
    public static final class Node1081<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1081";

        public Node1081(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 1081, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1082. */
    public static final class Node1082<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1082";

        public Node1082(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1083. */
    public static final class Node1083<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1083";

        public Node1083(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1083, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 1083, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1084. */
    public static final class Node1084<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1084";

        public Node1084(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1084, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1085. */
    public static final class Node1085<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1085";

        public Node1085(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1086. */
    public static final class Node1086<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1086";

        public Node1086(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1087. */
    public static final class Node1087<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1087";

        public Node1087(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1088. */
    public static final class Node1088<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1088";

        public Node1088(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1088, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1089. */
    public static final class Node1089<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1089";

        public Node1089(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1089, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1090. */
    public static final class Node1090<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1090";

        public Node1090(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1090, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1091. */
    public static final class Node1091<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1091";

        public Node1091(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1091, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1091, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1092. */
    public static final class Node1092<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1092";

        public Node1092(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1093. */
    public static final class Node1093<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1093";

        public Node1093(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1093, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1094. */
    public static final class Node1094<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1094";

        public Node1094(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1094, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 1094, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1095. */
    public static final class Node1095<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1095";

        public Node1095(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1095, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1096. */
    public static final class Node1096<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1096";

        public Node1096(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1097. */
    public static final class Node1097<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1097";

        public Node1097(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1097, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1098. */
    public static final class Node1098<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1098";

        public Node1098(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1099. */
    public static final class Node1099<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1099";

        public Node1099(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1099, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1100. */
    public static final class Node1100<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1100";

        public Node1100(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1100, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1101. */
    public static final class Node1101<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1101";

        public Node1101(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1101, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1102. */
    public static final class Node1102<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1102";

        public Node1102(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1102, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1103. */
    public static final class Node1103<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1103";

        public Node1103(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1103, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1104. */
    public static final class Node1104<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1104";

        public Node1104(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1105. */
    public static final class Node1105<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1105";

        public Node1105(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1105, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1106. */
    public static final class Node1106<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1106";

        public Node1106(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1106, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1107. */
    public static final class Node1107<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1107";

        public Node1107(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1108. */
    public static final class Node1108<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1108";

        public Node1108(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1109. */
    public static final class Node1109<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1109";

        public Node1109(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1109, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1110. */
    public static final class Node1110<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1110";

        public Node1110(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1110, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1110, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1111. */
    public static final class Node1111<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1111";

        public Node1111(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1111, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1112. */
    public static final class Node1112<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1112";

        public Node1112(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1113. */
    public static final class Node1113<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1113";

        public Node1113(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1114. */
    public static final class Node1114<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1114";

        public Node1114(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1114, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1115. */
    public static final class Node1115<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1115";

        public Node1115(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1115, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1116. */
    public static final class Node1116<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1116";

        public Node1116(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1116, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1116, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1117. */
    public static final class Node1117<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1117";

        public Node1117(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1117, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1118. */
    public static final class Node1118<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1118";

        public Node1118(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1118, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1119. */
    public static final class Node1119<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1119";

        public Node1119(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1119, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1120. */
    public static final class Node1120<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1120";

        public Node1120(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1120, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1120, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1121. */
    public static final class Node1121<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1121";

        public Node1121(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1121, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1122. */
    public static final class Node1122<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1122";

        public Node1122(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1123. */
    public static final class Node1123<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1123";

        public Node1123(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1123, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1124. */
    public static final class Node1124<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1124";

        public Node1124(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1124, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1124, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1125. */
    public static final class Node1125<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1125";

        public Node1125(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1125, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1125, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1126. */
    public static final class Node1126<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1126";

        public Node1126(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1127. */
    public static final class Node1127<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1127";

        public Node1127(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1128. */
    public static final class Node1128<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1128";

        public Node1128(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1129. */
    public static final class Node1129<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1129";

        public Node1129(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1130. */
    public static final class Node1130<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1130";

        public Node1130(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1130, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1131. */
    public static final class Node1131<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1131";

        public Node1131(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1132. */
    public static final class Node1132<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1132";

        public Node1132(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1132, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1132, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1133. */
    public static final class Node1133<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1133";

        public Node1133(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1134. */
    public static final class Node1134<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1134";

        public Node1134(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1135. */
    public static final class Node1135<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1135";

        public Node1135(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1135, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1136. */
    public static final class Node1136<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1136";

        public Node1136(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1137. */
    public static final class Node1137<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1137";

        public Node1137(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1138. */
    public static final class Node1138<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1138";

        public Node1138(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1139. */
    public static final class Node1139<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1139";

        public Node1139(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1140. */
    public static final class Node1140<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1140";

        public Node1140(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1141. */
    public static final class Node1141<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1141";

        public Node1141(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1142. */
    public static final class Node1142<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1142";

        public Node1142(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1143. */
    public static final class Node1143<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1143";

        public Node1143(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1143, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1144. */
    public static final class Node1144<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1144";

        public Node1144(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1145. */
    public static final class Node1145<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1145";

        public Node1145(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1146. */
    public static final class Node1146<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1146";

        public Node1146(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1147. */
    public static final class Node1147<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1147";

        public Node1147(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1148. */
    public static final class Node1148<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1148";

        public Node1148(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1148, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1149. */
    public static final class Node1149<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1149";

        public Node1149(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1150. */
    public static final class Node1150<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1150";

        public Node1150(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1151. */
    public static final class Node1151<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1151";

        public Node1151(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe3() {
            return """
                node 1151, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1152. */
    public static final class Node1152<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1152";

        public Node1152(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1152, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1152, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1153. */
    public static final class Node1153<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1153";

        public Node1153(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1154. */
    public static final class Node1154<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1154";

        public Node1154(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1154, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1155. */
    public static final class Node1155<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1155";

        public Node1155(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1155, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1156. */
    public static final class Node1156<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1156";

        public Node1156(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1157. */
    public static final class Node1157<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1157";

        public Node1157(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1157, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1158. */
    public static final class Node1158<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1158";

        public Node1158(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1158, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1159. */
    public static final class Node1159<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1159";

        public Node1159(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1159, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1160. */
    public static final class Node1160<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1160";

        public Node1160(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1160, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1161. */
    public static final class Node1161<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1161";

        public Node1161(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1161, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1162. */
    public static final class Node1162<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1162";

        public Node1162(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1162, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1163. */
    public static final class Node1163<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1163";

        public Node1163(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1164. */
    public static final class Node1164<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1164";

        public Node1164(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1165. */
    public static final class Node1165<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1165";

        public Node1165(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1165, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1166. */
    public static final class Node1166<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1166";

        public Node1166(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1167. */
    public static final class Node1167<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1167";

        public Node1167(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1167, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1168. */
    public static final class Node1168<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1168";

        public Node1168(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1169. */
    public static final class Node1169<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1169";

        public Node1169(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1170. */
    public static final class Node1170<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1170";

        public Node1170(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1171. */
    public static final class Node1171<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1171";

        public Node1171(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1171, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1171, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 1171, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1172. */
    public static final class Node1172<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1172";

        public Node1172(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1172, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1173. */
    public static final class Node1173<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1173";

        public Node1173(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1174. */
    public static final class Node1174<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1174";

        public Node1174(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1174, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1175. */
    public static final class Node1175<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1175";

        public Node1175(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1175, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1175, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1176. */
    public static final class Node1176<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1176";

        public Node1176(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1176, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1176, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1177. */
    public static final class Node1177<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1177";

        public Node1177(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1178. */
    public static final class Node1178<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1178";

        public Node1178(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1178, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1179. */
    public static final class Node1179<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1179";

        public Node1179(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1180. */
    public static final class Node1180<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1180";

        public Node1180(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1180, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1180, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1181. */
    public static final class Node1181<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1181";

        public Node1181(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1182. */
    public static final class Node1182<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1182";

        public Node1182(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1182, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1183. */
    public static final class Node1183<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1183";

        public Node1183(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1183, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1184. */
    public static final class Node1184<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1184";

        public Node1184(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1185. */
    public static final class Node1185<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1185";

        public Node1185(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1185, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1186. */
    public static final class Node1186<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1186";

        public Node1186(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1186, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1186, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1187. */
    public static final class Node1187<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1187";

        public Node1187(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1188. */
    public static final class Node1188<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1188";

        public Node1188(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1189. */
    public static final class Node1189<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1189";

        public Node1189(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1190. */
    public static final class Node1190<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1190";

        public Node1190(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1190, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1191. */
    public static final class Node1191<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1191";

        public Node1191(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1191, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1192. */
    public static final class Node1192<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1192";

        public Node1192(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1193. */
    public static final class Node1193<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1193";

        public Node1193(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1193, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1193, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1194. */
    public static final class Node1194<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1194";

        public Node1194(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1195. */
    public static final class Node1195<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1195";

        public Node1195(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1196. */
    public static final class Node1196<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1196";

        public Node1196(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1196, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1197. */
    public static final class Node1197<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1197";

        public Node1197(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1197, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1198. */
    public static final class Node1198<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1198";

        public Node1198(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe3() {
            return """
                node 1198, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1199. */
    public static final class Node1199<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1199";

        public Node1199(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1200. */
    public static final class Node1200<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1200";

        public Node1200(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 1200, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1201. */
    public static final class Node1201<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1201";

        public Node1201(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1202. */
    public static final class Node1202<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1202";

        public Node1202(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1202, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1203. */
    public static final class Node1203<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1203";

        public Node1203(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe2() {
            return """
                node 1203, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1204. */
    public static final class Node1204<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1204";

        public Node1204(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1204, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1204, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 1204, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1205. */
    public static final class Node1205<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1205";

        public Node1205(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1205, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1206. */
    public static final class Node1206<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1206";

        public Node1206(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1207. */
    public static final class Node1207<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1207";

        public Node1207(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1208. */
    public static final class Node1208<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1208";

        public Node1208(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1208, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1208, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1209. */
    public static final class Node1209<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1209";

        public Node1209(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1209, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1210. */
    public static final class Node1210<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1210";

        public Node1210(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1211. */
    public static final class Node1211<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1211";

        public Node1211(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1212. */
    public static final class Node1212<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1212";

        public Node1212(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1212, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1212, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1213. */
    public static final class Node1213<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1213";

        public Node1213(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1213, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1214. */
    public static final class Node1214<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1214";

        public Node1214(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1214, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1215. */
    public static final class Node1215<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1215";

        public Node1215(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1215, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1216. */
    public static final class Node1216<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1216";

        public Node1216(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1216, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1216, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1217. */
    public static final class Node1217<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1217";

        public Node1217(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1217, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1218. */
    public static final class Node1218<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1218";

        public Node1218(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1219. */
    public static final class Node1219<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1219";

        public Node1219(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1219, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1219, method 2
                values: %d
                """.formatted(values.size());
        }

        public String describe3() {
            return """
                node 1219, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1220. */
    public static final class Node1220<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1220";

        public Node1220(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1220, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1221. */
    public static final class Node1221<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1221";

        public Node1221(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1222. */
    public static final class Node1222<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1222";

        public Node1222(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1222, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1222, method 1
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1223. */
    public static final class Node1223<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1223";

        public Node1223(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1224. */
    public static final class Node1224<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1224";

        public Node1224(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1225. */
    public static final class Node1225<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1225";

        public Node1225(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1226. */
    public static final class Node1226<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1226";

        public Node1226(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1226, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1227. */
    public static final class Node1227<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1227";

        public Node1227(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1228. */
    public static final class Node1228<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1228";

        public Node1228(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1229. */
    public static final class Node1229<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1229";

        public Node1229(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total2(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1230. */
    public static final class Node1230<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1230";

        public Node1230(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1231. */
    public static final class Node1231<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1231";

        public Node1231(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1231, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1232. */
    public static final class Node1232<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1232";

        public Node1232(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1232, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1233. */
    public static final class Node1233<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1233";

        public Node1233(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1233, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1233, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1233, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1234. */
    public static final class Node1234<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1234";

        public Node1234(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1234, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1235. */
    public static final class Node1235<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1235";

        public Node1235(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1235, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1235, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1236. */
    public static final class Node1236<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1236";

        public Node1236(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1236, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1237. */
    public static final class Node1237<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1237";

        public Node1237(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1237, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1238. */
    public static final class Node1238<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1238";

        public Node1238(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1238, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1239. */
    public static final class Node1239<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1239";

        public Node1239(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1239, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe3() {
            return """
                node 1239, method 3
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1240. */
    public static final class Node1240<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1240";

        public Node1240(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1240, method 0
                values: %d
                """.formatted(values.size());
        }

        public String describe1() {
            return """
                node 1240, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1241. */
    public static final class Node1241<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1241";

        public Node1241(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1242. */
    public static final class Node1242<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1242";

        public Node1242(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1243. */
    public static final class Node1243<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1243";

        public Node1243(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1243, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1243, method 2
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1244. */
    public static final class Node1244<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1244";

        public Node1244(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1245. */
    public static final class Node1245<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1245";

        public Node1245(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1246. */
    public static final class Node1246<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1246";

        public Node1246(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1247. */
    public static final class Node1247<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1247";

        public Node1247(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1247, method 1
                values: %d
                """.formatted(values.size());
        }

        public String describe2() {
            return """
                node 1247, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1248. */
    public static final class Node1248<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1248";

        public Node1248(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1249. */
    public static final class Node1249<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1249";

        public Node1249(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1250. */
    public static final class Node1250<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1250";

        public Node1250(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1250, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1250, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1251. */
    public static final class Node1251<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1251";

        public Node1251(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1252. */
    public static final class Node1252<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1252";

        public Node1252(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1252, method 0
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe2() {
            return """
                node 1252, method 2
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1253. */
    public static final class Node1253<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1253";

        public Node1253(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1254. */
    public static final class Node1254<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1254";

        public Node1254(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1255. */
    public static final class Node1255<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1255";

        public Node1255(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1255, method 0
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1255, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1256. */
    public static final class Node1256<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1256";

        public Node1256(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1256, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1257. */
    public static final class Node1257<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1257";

        public Node1257(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1257, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1258. */
    public static final class Node1258<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1258";

        public Node1258(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1259. */
    public static final class Node1259<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1259";

        public Node1259(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public String describe0() {
            return """
                node 1259, method 0
                values: %d
                """.formatted(values.size());
        }

        @SafeVarargs
        public final int total1(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1260. */
    public static final class Node1260<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1260";

        public Node1260(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1261. */
    public static final class Node1261<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1261";

        public Node1261(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public Optional<T> pick0(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe1() {
            return """
                node 1261, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1262. */
    public static final class Node1262<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1262";

        public Node1262(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1262, method 1
                values: %d
                """.formatted(values.size());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

    /** Fixture type 1263. */
    public static final class Node1263<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1263";

        public Node1263(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1263, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1264. */
    public static final class Node1264<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1264";

        public Node1264(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public List<String> mapped1(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick2(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        @SafeVarargs
        public final int total3(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

    }

    /** Fixture type 1265. */
    public static final class Node1265<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1265";

        public Node1265(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1265, method 2
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1266. */
    public static final class Node1266<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1266";

        public Node1266(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        public List<String> mapped0(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public String describe1() {
            return """
                node 1266, method 1
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped2(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

        public Optional<T> pick3(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

    }

    /** Fixture type 1267. */
    public static final class Node1267<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1267";

        public Node1267(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public String describe1() {
            return """
                node 1267, method 1
                values: %d
                """.formatted(values.size());
        }

    }

    /** Fixture type 1268. */
    public static final class Node1268<T extends Comparable<T>> {
        private final List<T> values;
        private final Map<String, Integer> counts;
        private static final String LABEL = "node-1268";

        public Node1268(List<T> values, Map<String, Integer> counts) {
            this.values = values;
            this.counts = counts;
        }

        @SafeVarargs
        public final int total0(T... extra) {
            int sum = values.size();
            for (T ignored : extra) {
                sum += 1;
            }
            return sum;
        }

        public Optional<T> pick1(int bound) {
            if (values.isEmpty()) {
                return Optional.empty();
            }
            for (T value : values) {
                if (counts.getOrDefault(LABEL, 0) > bound) {
                    return Optional.of(value);
                }
            }
            return Optional.of(values.get(0));
        }

        public String describe2() {
            return """
                node 1268, method 2
                values: %d
                """.formatted(values.size());
        }

        public List<String> mapped3(Function<T, String> mapper) {
            return values.stream()
                .map(mapper)
                .filter(s -> !s.isBlank())
                .collect(Collectors.toList());
        }

    }

}
